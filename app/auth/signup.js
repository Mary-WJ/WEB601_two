//signup form
const signupForm = document.querySelector('#signupForm');

signupForm.addEventListener("submit", (e) => {
    e.preventDefault();

    alert("this one work too")
    //check credentials and transfer to the main page
})